name: db_benchmark
description: A new Flutter db benchmark.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  freezed_annotation:
  json_annotation: ^4.0.1
  hive: ^2.0.4
  sembast: ^3.0.2
  sqflite: ^2.0.0+3
  path_provider: ^2.0.1
  sembast_sqflite: ^2.0.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  moor_generator:
  build_runner:
  freezed:
  json_serializable: ^4.1.1

flutter:

  uses-material-design: true
